/*import * as express from 'express';


function headers(_req: Request, res: Response, next) {
  
    res.headers("Access-Control-Allow-Origin", "*"); // update to match the domain you will make the request from
  res.headers("Access-Control-Allow-Credentials", "true");
  res.headers(
    "Access-Control-Allow-Headers",
    "Origin, X-Requested-With, Content-Type, Accept"
  ); //headers que acepta el backend
  res.headers("Access-Control-Allow-Methods", "GET, POST, OPTIONS, PUT, DELETE"); //metodos permitidos
  next();
}

module.exports = headers;*/
